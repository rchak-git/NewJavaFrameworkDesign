# Shared Intent
Extend the existing payment creation smoke test so that after payment submission, the test verifies the created payment appears in `PaymentHistoryPage.paymentsTable` using the current Round2 framework design.

#